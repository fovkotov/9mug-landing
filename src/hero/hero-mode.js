/**
 * Switch product-page first-screen interaction.
 *
 * - "directional" → new mug-frame ProductViewer (mouse look-around)
 * - "legacy-slides" → previous click/drag slide hero (exact previous behavior)
 *
 * To restore the old first screen: set HERO_INTERACTION_MODE to "legacy-slides".
 */
export const HERO_INTERACTION_MODE = "directional";
